{
  "name": "ghostshell-license-frontend",
  "version": "1.0.0",
  "description": "Frontend for GhostShell License Server",
  "scripts": {
    "build": "mkdir -p build && cp -r src/* build/",
    "start": "npx serve -s build"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
